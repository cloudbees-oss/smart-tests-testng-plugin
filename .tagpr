[tagpr]
  releaseBranch = main
  versionFile = pom.xml
  tagPrefix = launchable-testng
  vPrefix = false
  changelog = false
  postVersionCommand = mvn --batch-mode versions:set -DnewVersion="${TAGPR_NEXT_VERSION##*/}" -DgenerateBackupPoms=false
